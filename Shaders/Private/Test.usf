#include "/Plugin/AtmosphericClouds/Test.ush"

void MainPS(
	in noperspective float4 UVAndScreenPos : TEXCOORD0,
	out float4 OutColor : SV_Target0)
{
	OutColor = float4(CloudScapesTestBinding(), 1.0);
}
